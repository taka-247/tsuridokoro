# if the pull brought changes to shared/src, rebuild shared/dist locally
if git diff-tree -r --name-only ORIG_HEAD HEAD | grep -q '^shared/src/'; then
  echo "shared/src changed upstream — running npm run share…"
  npm run share
fi